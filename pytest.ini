[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --strict-markers

markers =
    unit: unit tests for isolated functions/modules
    integration: integration tests exercising multiple components
    contract: contract tests for external interfaces (REST/MCP)
    property: property-based tests asserting invariants
    e2e: end-to-end tests through the running service
